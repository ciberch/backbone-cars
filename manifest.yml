---
applications:
  .:
    name: carsapp
    framework:
      name: rails3
      info:
        mem: 256M
        description: Rails Application
        exec: !!null 
    url: ${name}.${target-base}
    mem: 256M
    instances: 1
    services:
      carsapp_production:
        type: mysql
